{
  "name": "field-operation-dashboard",
  "private": true,
  "version": "1.0.0",
  "dependencies": {
    "firebase-admin": "^14.2.0"
  },
  "scripts": {
    "build": "cd frontend && npm install --legacy-peer-deps && npm run build"
  }
}
